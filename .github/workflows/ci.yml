name: CI

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  node:
    name: node ${{ matrix.node }}
    strategy:
      fail-fast: false
      matrix:
        # Cover the whole supported range — package.json declares "node >=20".
        node: [20, 22, 24]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js ${{ matrix.node }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Lint
        if: matrix.node == 24
        run: npm run lint

  trace:
    name: change trace
    # Only a pull request has a base to diff against. This is the one check that
    # asks a question about the change rather than about the tree, and it exists
    # because every omission this repository has had was a document moving while
    # its dependants did not.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Governing artifacts must say what they touched
        run: node scripts/check-change-trace.mjs origin/${{ github.base_ref }}

  nix:
    name: nix package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Flake check (package build + NixOS module VM test)
        run: nix flake check --no-update-lock-file -L

      - name: Smoke test the CLI wrapper
        run: nix run --no-update-lock-file .#teamclaude -- help

  # Single stable status check for branch protection to require, independent of
  # how many implementation jobs run. Always runs and fails explicitly if any
  # required job did not succeed (so it is never silently "skipped").
  test:
    name: test
    needs: [node, nix]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check job results
        if: needs.node.result != 'success' || needs.nix.result != 'success'
        run: |
          echo "Node job result: ${{ needs.node.result }}"
          echo "Nix package result: ${{ needs.nix.result }}"
          exit 1
      - run: echo "All required jobs passed"
